apiVersion: v1
kind: Pod
metadata:
  name: test-gpu-pod
  namespace: fuddin-dev
  labels:
    app: test-gpu
spec:
  containers:
  - name: cuda-test
    image: nvidia/cuda:11.8.0-base-ubuntu22.04
    command: ["sleep", "infinity"]
    resources:
      limits:
        nvidia.com/gpu: 1
